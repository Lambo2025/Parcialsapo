export class Pais {
nombre: string;
habitantes: number;
moneda: string;

 public constructor(nombre: string, habitantes: number, moneda: string) {
   this.nombre = nombre;
   this.habitantes = habitantes;
   this.moneda = moneda;
 }
}
